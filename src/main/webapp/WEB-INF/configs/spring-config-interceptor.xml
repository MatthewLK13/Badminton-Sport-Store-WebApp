<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <context:component-scan base-package="com.sport" />

    <mvc:annotation-driven>
    <mvc:message-converters>
        <bean class="org.springframework.http.converter.StringHttpMessageConverter">
            <property name="supportedMediaTypes">
                <list>
                    <value>text/plain;charset=UTF-8</value>
                    <value>application/json;charset=UTF-8</value>
                </list>
            </property>
        </bean>
        <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter"/>
    </mvc:message-converters>
</mvc:annotation-driven>

    <!-- Interceptor Beans -->
    <bean id="loggingInterceptor" class="com.sport.interceptor.LoggingInterceptor" />
    <bean id="authInterceptor" class="com.sport.interceptor.AuthInterceptor" />
    <bean id="adminInterceptor" class="com.sport.interceptor.AdminInterceptor" />
    <bean id="cartInterceptor" class="com.sport.interceptor.CartInterceptor">
        <property name="cartDao" ref="cartDao" />
    </bean>

    <!-- Message Source for i18n -->
    <bean id="messageSource" class="org.springframework.context.support.ResourceBundleMessageSource">
        <property name="basename" value="messages" />
        <property name="defaultEncoding" value="UTF-8" />
    </bean>

    <!-- Session Locale Resolver -->
    <bean id="localeResolver" class="org.springframework.web.servlet.i18n.SessionLocaleResolver">
        <property name="defaultLocale" value="en" />
    </bean>

    <!-- Locale Change Interceptor -->
    <bean id="localeChangeInterceptor" class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor">
        <property name="paramName" value="lang" />
    </bean>

    <mvc:interceptors>
        <!-- Locale Change Interceptor -->
        <mvc:interceptor>
            <mvc:mapping path="/changeLocale.htm" />
            <mvc:mapping path="/**" />
            <mvc:exclude-mapping path="/static/**" />
            <mvc:exclude-mapping path="/css/**" />
            <mvc:exclude-mapping path="/js/**" />
            <mvc:exclude-mapping path="/images/**" />
            <ref bean="localeChangeInterceptor" />
        </mvc:interceptor>

        <!-- Logging - chạy cho tất cả request -->
        <mvc:interceptor>
            <mvc:mapping path="/**" />
            <mvc:exclude-mapping path="/static/**" />
            <mvc:exclude-mapping path="/css/**" />
            <mvc:exclude-mapping path="/js/**" />
            <mvc:exclude-mapping path="/images/**" />
            <bean class="com.sport.interceptor.LoggingInterceptor" />
        </mvc:interceptor>

        <!-- Auth - bảo vệ các route cần đăng nhập -->
        <mvc:interceptor>
            <mvc:mapping path="/profile.htm" />
            <mvc:mapping path="/order/**" />
            <mvc:mapping path="/checkout/**" />
            <mvc:mapping path="/wishlist/**" />
            <mvc:mapping path="/review/**" />
            <mvc:mapping path="/cart/**" />
            <mvc:exclude-mapping path="/login.htm" />
            <mvc:exclude-mapping path="/register.htm" />
            <mvc:exclude-mapping path="/forgot.htm" />
            <bean class="com.sport.interceptor.AuthInterceptor" />
        </mvc:interceptor>

        <!-- Admin - bảo vệ route admin -->
        <mvc:interceptor>
            <mvc:mapping path="/admin/**" />
            <bean class="com.sport.interceptor.AdminInterceptor" />
        </mvc:interceptor>

        <!-- Cart - load cart count cho mọi request -->
        <mvc:interceptor>
            <mvc:mapping path="/**" />
            <mvc:exclude-mapping path="/static/**" />
            <mvc:exclude-mapping path="/css/**" />
            <mvc:exclude-mapping path="/js/**" />
            <mvc:exclude-mapping path="/images/**" />
            <bean class="com.sport.interceptor.CartInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>

    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="5242880" />
        <property name="defaultEncoding" value="UTF-8" />
    </bean>

</beans>